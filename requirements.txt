# Core dependencies for Simple Token Burner App
# This file is for reference - the project uses uv for dependency management

# HTTP requests
requests>=2.25.0

# Optional dependencies for specific providers (install as needed)
# openai>=1.0.0
# anthropic>=0.3.0

# Development dependencies
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0